version: "3.3"
services:
  app:
    build: .
    restart: always
    network_mode: host
    environment:
      PG_URL: postgresql://postgres:***REMOVED***@db.example.com:5432/random_message
      ENV_VALID: "true"
      BOT_API_KEY: ***REMOVED***